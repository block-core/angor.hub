import { Routes } from '@angular/router';
import { AuthResetPasswordComponent } from 'app/components/auth/reset-password/reset-password.component';

export default [
    {
        path: '',
        component: AuthResetPasswordComponent,
    },
] as Routes;
